linters:
  disable-all: true
  enable:
    # All code is ready for:
    - errcheck
    - staticcheck
    - typecheck
    - unused
    - misspell
    - nolintlint
    - goimports
    - nakedret
    - unconvert
    - whitespace
    # ToDo:
    #- gosimple
    #- govet
    #- ineffassign
    #- gocritic
    #- golint
linters-settings:
  nolintlint:
    allow-unused: false
    allow-leading-space: false
    require-specific: true
